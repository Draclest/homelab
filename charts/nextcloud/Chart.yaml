apiVersion: v2
name: nextcloud
description: Nextcloud with MariaDB and Redis
type: application
version: 0.1.0
appVersion: "33.0.3"

dependencies:
  - name: nextcloud
    version: "9.1.0"
    repository: "https://nextcloud.github.io/helm/"
