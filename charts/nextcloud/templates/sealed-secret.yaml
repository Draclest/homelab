# Ce fichier est généré via kubeseal depuis le NAS.
# Commandes à exécuter sur le NAS pour (re)générer :
#
# kubectl create secret generic nextcloud-credentials \
#   --namespace=nextcloud \
#   --from-literal=nextcloud-username=<ADMIN_USER> \
#   --from-literal=nextcloud-password=<ADMIN_PASSWORD> \
#   --from-literal=mariadb-root-password=<MARIADB_ROOT_PASSWORD> \
#   --from-literal=mariadb-password=<MARIADB_PASSWORD> \
#   --from-literal=smtp-username=<SMTP_USER> \
#   --from-literal=smtp-password=<SMTP_PASSWORD> \
#   --dry-run=client -o yaml | \
#   kubeseal --controller-namespace=sealed-secrets \
#            --controller-name=sealed-secrets \
#            --format=yaml > charts/nextcloud/templates/sealed-secret.yaml
#
# REMPLACER CE COMMENTAIRE PAR LA SORTIE DE kubeseal
