apiVersion: v2
name: sealed-secrets
description: Sealed Secrets controller for homelab
type: application
version: 0.1.0
appVersion: "0.28.0"

dependencies:
  - name: sealed-secrets
    version: "2.18.0"
    repository: "https://bitnami-labs.github.io/sealed-secrets"
